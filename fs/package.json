{
  "name": "@jotcad/fs",
  "version": "0.0.1",
  "description": "Distributed, idempotent, stream-based virtual filesystem",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=4 --test-timeout=30000 --test-force-exit test/*.test.js"
  },
  "devDependencies": {
    "puppeteer": "^24.40.0",
    "ws": "^8.20.0"
  },
  "dependencies": {
    "eventsource": "^4.1.0",
    "memfs": "^4.57.1",
    "@eclipse-zenoh/zenoh-ts": "^1.9.0"
  }
}
