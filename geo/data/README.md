# Geometry Data Models (geo/data)

Fundamental data structures for the JotCAD geometry domain.

- **Responsibilities**: Define the `Shape` hierarchy and the `Geometry` (mesh) model.
- **Key Files**: `shape.h`, `geometry.h`.
