# Geometry Data Models (geo/data)

Fundamental data structures for the JotCAD geometry domain.

- **Responsibilities**: Define the `Shape` hierarchy, the `Geometry` container, and conversions between `Geometry` and CGAL `Surface_mesh`.
- **Key Files**: `shape.h`, `geometry.h`, `surface_mesh_geometry.h`.

